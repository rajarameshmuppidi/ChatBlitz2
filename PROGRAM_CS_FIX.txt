// COPY THIS CODE TO Program.cs
// Replace the line: var app = builder.Build();
// With this complete section:

var app = builder.Build();

// Configure the HTTP request pipeline - ENABLE SWAGGER UI HERE
if (app.Environment.IsDevelopment())
{
    app.UseSwagger();
    app.UseSwaggerUI();
}

app.UseHttpsRedirection();
app.UseCors("AllowAll");
app.UseAuthentication();
app.UseAuthorization();

app.MapControllers();
app.MapHub<ChatHub>("/chathub").RequireCors("SignalRCors");

app.Run();
